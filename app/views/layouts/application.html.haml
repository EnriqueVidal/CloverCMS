!!!
%html
  %head
    = javascript_include_tag  :defaults
    = calendar_date_select_includes "silver"
    = stylesheet_link_tag     "scaffold"
    %title
      = params[:title]
  %body
    .messages
      - if flash[:notice]
        #flash_notice
          = flash[:notice]

      - if flash[:error]
        #flash_error
          = flash[:error]

      - if flash[:success]
        #flash_success
          = flash[:success]

    .content
      = yield