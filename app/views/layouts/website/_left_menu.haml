#imid
  #column
    %h2.sb_header= current_page.title
    .sidebar
      %ul.services
        - pages.each do |page|
          %li= link_to page.title, show_section_page_path( :section_name => page.pageable.name, :page_name => page.name)
      %p.right= link_to "<span>More</span>", "#", :class => "button2"
    .sidebar_bottom
  #content= yield